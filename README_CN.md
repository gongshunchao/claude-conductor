# Claude Conductor

[English](README.md) | **中文**

[![版本](https://img.shields.io/badge/version-1.3.0-blue.svg)](CHANGELOG.md)
[![许可证](https://img.shields.io/badge/license-Apache--2.0-green.svg)](LICENSE)
[![Claude Code](https://img.shields.io/badge/Claude%20Code-plugin-purple.svg)](https://docs.anthropic.com/en/docs/claude-code)

Claude Code 的上下文驱动开发框架。

> 灵感来源于 [Conductor for Gemini CLI](https://github.com/gemini-cli-extensions/conductor)

## 概述

Conductor 实现了**上下文驱动开发** —— 一种将上下文作为与代码并行管理的工件的方法论。它提供：

- **预实现规划**：在编码前生成规格说明和计划
- **测试驱动开发**：强制执行 红-绿-重构 循环
- **上下文管理**：维护代码风格指南、技术栈、产品目标
- **代码审查**：基于指南、风格指南和规格说明的严重性分级审查
- **迭代安全**：在执行前审查计划，设置检查点
- **智能回退**：基于 Git 的逻辑工作单元回滚
- **会话恢复**：跨会话自动恢复工作

## 快速开始

```bash
# 1. 使用插件启动 Claude Code
claude --plugin-dir /path/to/conductor

# 2. 初始化你的项目
/conductor:setup

# 3. 创建你的第一个功能轨道
/conductor:new-track 添加用户认证

# 4. 使用 TDD 实现
/conductor:implement

# 5. 审查已完成的工作
/conductor:review
```

**完整生命周期**：设置 → 规划 → 实现 → 审查 → 归档

## 安装

### 开发 / 测试

使用 `--plugin-dir` 标志直接加载插件：

```bash
claude --plugin-dir /path/to/conductor
```

**验证安装：**
```bash
# 使用插件启动 Claude Code
claude --plugin-dir /path/to/conductor

# 输入 /conductor: 查看可用命令
/conductor:
```

你应该能看到列出的 7 个 conductor 命令。

### 生产环境（通过 GitHub Marketplace）

将 GitHub 仓库添加为 marketplace 并安装：

```bash
# 将此仓库添加为 marketplace
/plugin marketplace add gongshunchao/claude-conductor

# 安装插件
/plugin install conductor@claude-conductor
```

### 符号链接安装（替代方案）

```bash
# 如果不存在则创建插件目录
mkdir -p ~/.claude/plugins

# 创建 conductor 插件的符号链接
ln -s /path/to/conductor ~/.claude/plugins/conductor
```

## 命令

### `/conductor:setup`

为项目初始化 Conductor 环境。

**用法：**
```
/conductor:setup [brownfield|greenfield]
```

**参数：**
- `brownfield` - 跳过检测，视为已有项目
- `greenfield` - 跳过检测，视为新项目

**功能说明：**
1. 检测项目成熟度（brownfield 已有项目 vs greenfield 新项目）
2. 创建 `conductor/` 目录及上下文文件
3. 交互式生成 `product.md`、`tech-stack.md`、`workflow.md`
4. 创建第一个轨道，包含规格说明和计划

**示例：**
```
> /conductor:setup

检测到已有项目。我将分析它以了解当前状态。
发现：package.json, src/, .git

你想构建什么？[描述你的第一个功能]
```

---

### `/conductor:new-track`

创建新的功能、缺陷修复或杂项轨道。

**用法：**
```
/conductor:new-track [描述]
```

**参数：**
- `描述` - 工作的简要描述（可选，未提供时会提示输入）

**功能说明：**
1. 从描述中推断轨道类型（功能/缺陷/杂项）
2. 委托规划代理进行交互式问答
3. 生成包含需求的 `spec.md`
4. 生成包含 TDD 结构化任务的 `plan.md`
5. 创建轨道目录并更新 `tracks.md`

**示例：**
```
> /conductor:new-track 添加 OAuth 用户认证

正在创建功能轨道...
我有一些问题需要澄清需求：
1. 应该支持哪些 OAuth 提供商？
2. 是否需要包含"记住我"功能？
...
```

---

### `/conductor:implement`

按照 TDD 工作流执行任务。

**用法：**
```
/conductor:implement [<轨道名称>] [--all]
```

**参数：**
- `<轨道名称>` - 部分名称以选择特定轨道（可选，默认选择下一个未完成的）
- `--all` - 运行所有阶段而非逐个执行

**功能说明：**
1. 选择下一个未完成的轨道（或指定的轨道）
2. **单阶段模式（默认）**：显示交互式阶段选择，逐个实现阶段
3. **全阶段模式（`--all`）**：连续实现所有剩余阶段
4. 将每个任务委托给实现代理
5. 遵循 TDD 循环：红 → 绿 → 重构
6. 创建带有 git notes 的原子提交
7. 在阶段边界触发阶段验证
8. 通过审查代理创建检查点

**示例：**
```
> /conductor:implement

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  阶段选择
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  轨道：用户认证

  [x] 阶段 1：设置                    [已完成]
  [ ] 阶段 2：后端 API              [进行中 - 2/5 任务]
  [ ] 阶段 3：前端集成              [待处理]

  你想实现哪个阶段？

  A) 阶段 2：后端 API（推荐）
  B) 阶段 3：前端集成
  C) 所有剩余阶段
```

```
> /conductor:implement --all

正在运行轨道"用户认证"的所有剩余阶段。
当前任务：实现 JWT 验证

[红] 编写失败测试...
[绿] 实现以通过测试...
[重构] 清理代码...

任务完成。提交：feat(auth): 实现 JWT 验证 [a1b2c3d]
```

---

### `/conductor:review`

根据指南、风格指南和计划审查已完成的工作。

**用法：**
```
/conductor:review [<轨道名称>]
```

**参数：**
- `<轨道名称>` - 要审查的轨道名称（可选，未提供时会提示输入）

**功能说明：**
1. 加载产品指南、代码风格指南和轨道上下文
2. 提取与轨道关联的所有提交
3. 根据风格指南（高严重性）、规格合规性和安全性审查代码
4. 生成严重性分级的发现报告
5. 提供自动修复或暂停手动解决的选项
6. 提供轨道清理选项（归档/删除/跳过）

**示例：**
```
> /conductor:review user-auth

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  CONDUCTOR 审查报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  轨道：用户认证
  审查文件数：12
  分析提交数：8

🟠 高严重性 (2)
  1. [风格指南] 认证中间件缺少错误处理
  2. [规格] 未按规格实现速率限制

🟡 中严重性 (1)
  1. [指南] 登录按钮文本不符合品牌语调

你想怎么做？
A) 应用修复  B) 手动修复  C) 接受并继续
```

---

### `/conductor:status`

显示项目进度报告。

**用法：**
```
/conductor:status
```

**功能说明：**
1. 解析所有轨道和计划
2. 计算完成百分比
3. 识别当前焦点（进行中的轨道/阶段/任务）
4. 列出所有阻塞项
5. 显示下一步操作

**示例输出：**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  CONDUCTOR 状态报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前焦点
───────────────────────────────────────────────────
  轨道：用户认证
  阶段：后端 API [~]
  任务：实现 JWT 验证 [~]

进度
───────────────────────────────────────────────────
  [████████████░░░░░░░░] 60%  (12/20 任务)
```

---

### `/conductor:agents`

管理后台 Conductor 代理和 git 工作树。

**用法：**
```
/conductor:agents [status|results|worktrees|cleanup]
```

**参数：**
- `status`（默认）- 检查后台代理状态
- `results` - 获取已完成代理的结果
- `worktrees` - 列出用于隔离的 git 工作树
- `cleanup` - 清理孤立的工作树

**功能说明：**
1. 列出正在运行的后台代理（实现者、规划者、审查者）
2. 显示工作树隔离状态
3. 获取已完成代理的结果
4. 合并并清理工作树分支

**示例：**
```
> /conductor:agents

后台代理：
  [运行中] 实现者 - 任务：添加用户验证 (工作树: .worktrees/implementer-1735075200)

工作树：
  .worktrees/implementer-1735075200 → conductor/implementer-1735075200
```

```
> /conductor:agents results

实现者已完成：
  ✓ 3 个任务已完成
  ✓ 4 个提交已创建
  ✓ 检查点：a1b2c3d

正在将工作树合并回主分支...
```

---

### `/conductor:revert`

基于 Git 的轨道、阶段或任务回滚。

**用法：**
```
/conductor:revert [track|phase|task] [名称]
```

**参数：**
- `track <名称>` - 回退整个轨道
- `phase <名称>` - 回退特定阶段
- `task <名称>` - 回退特定任务

**功能说明：**
1. 识别与目标关联的所有提交
2. 查找实现和计划更新的提交
3. 展示回退计划以供确认
4. 按正确顺序执行 `git revert`
5. 处理冲突并提供用户指导
6. 更新 plan.md 状态标记

**示例：**
```
> /conductor:revert phase "后端 API"

我已分析了 git 历史。以下是回退计划：

要回退的提交（从最新到最旧）：
  1. b2c3d4e - conductor(plan): 完成 '添加速率限制'
  2. a1b2c3d - feat(api): 为端点添加速率限制

是否继续？[Y/n]
```

## 配置

### 工作流自定义

设置完成后编辑 `conductor/workflow.md` 进行自定义：

| 设置 | 默认值 | 描述 |
|------|--------|------|
| 覆盖率目标 | 80% | 最低测试覆盖率百分比 |
| 提交策略 | 按任务 | 何时提交（按任务或按阶段） |
| Git Notes | 启用 | 为提交附加摘要 |
| 移动端测试 | 视情况而定 | 要求移动端验证 |
| 并行代理 | worktree | 并行代理执行策略 |

### 质量门禁

工作流在任务完成前强制执行以下检查：

- 所有测试通过
- 代码覆盖率达标（>80%）
- 代码遵循风格指南
- 无 lint 错误
- 类型安全强制执行
- 文档已更新

### 代码风格指南

语言特定的风格指南位于 `templates/code-styleguides/`：

- TypeScript
- Python
- Go
- JavaScript
- HTML/CSS
- C#
- 通用（通用标准）

在 `/conductor:setup` 期间，会根据你的技术栈将选定的风格指南复制到 `conductor/code_styleguides/`。`code-styleguides` 技能在编写代码时自动激活，从你项目的风格指南文件中读取。

## 会话恢复

Conductor 在启动新会话时自动恢复工作：

1. **SessionStart 钩子** 检测 conductor 项目并显示当前状态
2. **plan.md 状态标记** 跟踪进度（`[ ]` 待处理，`[~]` 进行中，`[x]` 已完成）
3. **运行 `/conductor:implement`** 从上次中断的地方继续

无需特殊的交接文件 —— plan.md 是唯一的事实来源。

## 后台执行与并行代理

Conductor 支持在后台运行代理以处理长时间运行的任务和并行工作流。

### 何时使用后台执行

```bash
# 在后台运行实现者处理长任务
> 我想实现用户验证。你能在后台运行它，同时我处理其他事情吗？

# 并行运行多个任务
> 在后台开始实现认证，我来处理前端
```

### 并行代理策略

在 `conductor/workflow.md` 中配置：

| 策略 | 描述 | 使用场景 |
|------|------|----------|
| **worktree**（推荐） | 每个后台代理在隔离的 git 工作树中工作 | 无冲突的并行开发 |
| **sequential** | 后台代理排队，逐个运行 | 不需要工作树时的安全回退 |
| **unsafe** | 无隔离，代理在同一分支上工作 | 不推荐（需手动解决冲突） |

### 工作树隔离的工作原理

当 `parallel_agents: worktree` 时：

1. **代理启动**：创建 `.worktrees/implementer-<id>/` 及独立分支
2. **隔离工作**：所有文件更改在工作树目录中进行
3. **完成时**：分支自动合并回主分支
4. **清理**：合并后移除工作树和分支

**优势：**
- ✅ 并行执行期间无 git 冲突
- ✅ 每个代理拥有独立工作空间
- ✅ 使用 `--no-ff` 的干净合并历史
- ✅ 成功后自动清理

**示例：**
```bash
> /conductor:agents

后台代理：
  [运行中] 实现者 - 工作树: .worktrees/implementer-1735075200

# 代理完成...

> /conductor:agents results

实现者已完成。正在合并工作树...
✓ 已合并 conductor/implementer-1735075200 → main
✓ 已移除工作树
```

## 项目结构

### 插件结构

```
claude-conductor/
├── .claude-plugin/
│   ├── plugin.json           # 插件清单（名称、版本、入口点）
│   └── marketplace.json      # 插件市场元数据
├── agents/
│   ├── planner.md            # 规划代理（规格说明和计划生成）
│   ├── implementer.md        # 实现代理（TDD 执行）
│   └── reviewer.md           # 审查代理（检查点和代码审查）
├── commands/
│   ├── setup.md              # /conductor:setup
│   ├── new-track.md          # /conductor:new-track
│   ├── implement.md          # /conductor:implement
│   ├── review.md             # /conductor:review
│   ├── status.md             # /conductor:status
│   ├── agents.md             # /conductor:agents
│   └── revert.md             # /conductor:revert
├── skills/
│   ├── context-awareness/    # 自动加载项目上下文
│   ├── tdd-workflow/         # TDD 红-绿-重构 指导
│   └── code-styleguides/     # 语言特定的风格规则
├── hooks/
│   ├── hooks.json            # 钩子事件配置
│   └── scripts/              # 钩子脚本（上下文加载、跟踪）
├── templates/
│   └── code-styleguides/     # 风格指南模板（TS、Python、Go、JS、CSS、C#、通用）
├── reference/                # 详细架构文档
├── CHANGELOG.md
├── LICENSE
└── README.md
```

### 项目结构（`/conductor:setup` 之后）

```
your-project/
└── conductor/
    ├── product.md           # 产品愿景和目标
    ├── product-guidelines.md # 品牌和设计标准
    ├── tech-stack.md        # 技术选型
    ├── workflow.md          # 开发方法论
    ├── code_styleguides/    # 为你的技术栈复制的风格指南
    ├── setup_state.json     # 恢复能力状态
    ├── tracks.md            # 轨道概览
    └── tracks/
        └── <track_id>/
            ├── spec.md      # 需求规格说明
            ├── plan.md      # 实现计划
            └── metadata.json
```

## 架构

Conductor 使用 Claude Code 的插件系统：

| 组件 | 用途 | 详情 |
|------|------|------|
| **命令** (`/conductor:*`) | 用户调用的操作 | 7 个命令：setup、new-track、implement、review、status、agents、revert |
| **代理** | 专业化子代理 | 规划者、实现者、审查者 —— 各自预加载技能 |
| **技能** | 自动加载的能力 | 上下文感知、TDD 工作流、代码风格指南 |
| **钩子** | 事件驱动的自动化 | SessionStart（上下文加载）、PostToolUse（变更跟踪） |
| **工作树** | Git 隔离 | 无冲突的并行代理执行 |

### 技能自动加载

代理在启动时预加载技能以加快上下文加载：

| 代理 | 预加载的技能 |
|------|-------------|
| **规划者** | 上下文感知 |
| **实现者** | 上下文感知、TDD 工作流、代码风格指南 |
| **审查者** | 上下文感知 |

这确保代理能立即访问：
- 项目上下文（product.md、tech-stack.md、workflow.md）
- TDD 方法论（红-绿-重构循环）
- 语言特定的风格指南

## 故障排除

### 前置条件

使用 Conductor 之前：

1. **Git 仓库**：项目必须是 git 仓库（否则 `/conductor:setup` 会初始化一个）
2. **Claude Code**：支持插件的版本
3. **测试框架**：项目应已设置测试框架以支持 TDD 工作流

### 常见错误

**"Conductor 未设置"**
```
请先运行 /conductor:setup 初始化 conductor/ 目录。
```

**"未找到轨道"**
```
运行 /conductor:new-track 创建你的第一个轨道。
```

**"你有未提交的更改"**
```
在运行 /conductor:revert 之前，请提交或暂存你的更改。
```

### 恢复中断的设置

如果设置被中断，可以恢复：

```bash
# 设置状态保存在 conductor/setup_state.json 中
# 重新运行 setup 将提供从上次成功步骤恢复的选项
/conductor:setup
```

### 调试模式

获取显示钩子执行的详细输出：

```bash
claude --verbose --plugin-dir /path/to/conductor
```

检查钩子执行日志：
```
~/.claude/debug/
```

## 文档

详细文档请参阅 `reference/` 目录：

- [实现蓝图](reference/implementation-blueprint.md)
- [插件架构](reference/plugin-architecture.md)
- [Claude Code 映射](reference/claude-code-mapping.md)
- [Conductor 分析](reference/conductor-analysis.md)

## 贡献

欢迎贡献！以下是入门方法：

1. **Fork** 此仓库
2. **创建分支**：`git checkout -b feat/my-feature`
3. **进行更改**，遵循 [通用风格指南](templates/code-styleguides/general.md)
4. **使用 Claude Code 测试**：`claude --plugin-dir /path/to/your-fork`
5. **提交**，使用 [约定式提交](https://www.conventionalcommits.org/)：`feat: 添加新功能`
6. **提交 Pull Request**，附上清晰的描述

### 开发提示

- 命令是 `commands/` 中的 Markdown 文件 —— 编辑后重新加载即可测试
- 技能使用 frontmatter 定义激活规则 —— 参见 `skills/*/SKILL.md`
- 钩子在 `hooks/hooks.json` 中配置，脚本位于 `hooks/scripts/`
- 从项目根目录运行 `claude --plugin-dir .` 测试更改

## 许可证

[Apache-2.0](LICENSE)
